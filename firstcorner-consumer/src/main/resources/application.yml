server:
  port: 6001 #服务消费者

#指定当前eureka客户端的注册地址
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:11111/eureka
  instance:
    hostname: localhost

spring:
  application:
    name: easy-calc-consumer
